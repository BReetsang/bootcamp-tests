function greet(name, language) {

    if (language === "Setswana") {
        return "Dumela, " + name;

    } else if (language === "English") {
        return "Hello, " + name;
    }
    return "Ndaa, " + name;
}